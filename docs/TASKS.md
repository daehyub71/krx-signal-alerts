# TASKS.md — krx-signal-alerts

> 기준: `SPEC.md` v1.1 · `PLAN.md` (2026-08-17, LangGraph 반영)
> 체크할 때마다 아래 대시보드를 함께 갱신한다.

---

## 진도율 대시보드

| 마일스톤 | 진도 | % | 태스크 | 상태 |
|----------|------|---|--------|------|
| M0 뼈대 + 걷는 해골 | `██████████` | 100% | 10/10 | ✅ 2026-08-17 |
| M1 데이터 계층 | `██████████` | 100% | 8/8 | ✅ 2026-08-17 |
| M2 전략 5개 | `██████████` | 100% | 14/14 | ✅ 2026-08-17 |
| M3 발송 | `████████░░` | 80% | 8/10 | 🔄 카카오 인가 대기 |
| M4 자동화 | `░░░░░░░░░░` | 0% | 0/7 | 🔜 |
| M5 웹 /signals | `░░░░░░░░░░` | 0% | 0/9 | 🔜 |
| M6 마무리 | `░░░░░░░░░░` | 0% | 0/6 | 🔜 |
| **전체** | `██████░░░░` | **63%** | **40/64** | 🔄 |

범례: 🔜 대기 · 🔄 진행중 · ✅완료일

---

## M0 — 뼈대 + 걷는 해골

> **그래프를 먼저 세우고 노드를 나중에 채운다.** 노드를 다 만든 뒤 조립하면
> reducer·조건부 엣지·병렬 합류가 한꺼번에 터진다 (PLAN §5 M0)

- [x] venv 생성 · `requirements.txt`(**langgraph** · supabase) · `requirements-dev.txt`(ruff·mypy·pytest)
- [x] `pyproject.toml` — ruff 설정 + mypy strict
- [x] `.gitignore` — `.env` · `.tokens.json` · `venv/` · `__pycache__` · `.DS_Store`
- [x] `.env.example` — 시크릿 **키 이름만** (값 금지)
- [x] `alerts/config.py`(표준 라이브러리 로더, **기존 환경변수 덮어쓰지 않음** — CI 우선) · `alerts/models.py`
- [x] `alerts/state.py` — `AlertState` TypedDict + **`signals` reducer(`operator.add`)** + `results` merge reducer
- [x] `alerts/nodes.py` — 노드 전부를 **빈 통과 함수**로 (PLAN §1-1 구조 그대로)
- [x] `alerts/graph.py` — `build_graph()`: 노드·엣지·`check_freshness` 조건부 분기·`RetryPolicy`
- [x] `alerts/main.py` — 인자 파싱 → 초기 상태 → `graph.invoke()`. `--dry-run` 완주 확인
- [x] `scripts/export_graph.py` → `docs/GRAPH.md` (N12) · `git init` + 최초 커밋

**완료 기준 — 전부 충족 (2026-08-17)**
- `ruff check .` · `mypy` · `pytest` 전부 통과 (**테스트 28개**)
- 빈 그래프가 START→END 완주 (`python -m alerts.main --dry-run` → `status=ok`, exit 0)
- 분기 양쪽 경로 · **리듀서 5개 합류** · 발송 실패 격리 전부 테스트로 확인
- `docs/GRAPH.md`에 mermaid 다이어그램 생성됨 (노드 16 + START/END)

**M0에서 확정한 것**

| 항목 | 내용 |
|------|------|
| LangGraph 버전 | **1.2.11** (PLAN 작성 시 가정한 0.2 계열이 아니다). `StateGraph`·`add_conditional_edges`·`RetryPolicy` API는 그대로 호환 |
| 프레임워크 경계 | `add_node` 오버로드가 평범한 `Callable`을 strict 모드에서 거부한다. `graph.pick()`의 반환형만 `Any`로 완화하고 나머지는 strict 유지 |
| 테스트 주입구 | `build_graph(overrides)` — 노드를 스텁으로 갈아끼워 **배선만** 검사한다 (N11) |
| 예외 이름 | `AlertRunFailed` → **`AlertRunError`** (ruff N818) |

---

## M1 — 데이터 계층

- [x] `supabase/schema.sql` 작성 (`ksa_signals` · `ksa_runs` · RLS, 멱등)
- [x] 스키마 적용 (`scripts/apply_schema.py` — psycopg + `SUPABASE_DATABASE_URL`) ✅ 2026-08-17
      · 재적용 멱등 확인 · service_role upsert 성공 · **anon 쓰기 RLS 차단 확인** · CHECK 3종 동작
- [x] `alerts/store.py` — psycopg 대량 조회 + supabase-py 쓰기 (아래 「달라진 것」 ①)
- [x] `alerts/indicators.py` — `sma` · `bollinger` · `quantile` (**테스트 먼저** · 16개)
- [x] `alerts/universe.py` — F1 필터 (스팩 · 우선주 · 유동성 미달 · 데이터 없음) · 테스트 12개
- [x] `alerts/freshness.py` — F3 판정, 계획에 없던 모듈 (아래 ②) · 테스트 9개
- [x] **유니버스 건수를 SQL 직접 카운트와 대조** (REST로 세지 않는다 — R6) → 2,763 일치 ✓
- [x] `load_meta` · `check_freshness` · `build_universe` · `load_bars` 노드에 실구현 연결
- [x] 전 종목 조회 **소요 시간 + `bars` 상태 메모리** 측정 (R7 · PLAN §1-2)
- [x] `scripts/measure_m1.py` — 위 측정을 언제든 재현한다

**완료 기준 — 전부 충족 (2026-08-17)**
- 절단 없는 조회 확인 (SQL 카운트 2,763 = 조회 2,763)
- 제외 사유별 건수 로그 출력 확인
- 검증 3종 통과 (**테스트 65개**)
- **실DB 완주**: `python -m alerts.main --dry-run` → **50.5초**, `status=ok`, 유니버스 1,192종목
- **신선도 게이트 실동작**: `--date 20260821`(데이터보다 4영업일 뒤) → `status=stale_data`,
  신호 계산을 건너뛰고 발송 경로로 합류하는 것까지 확인

**M1에서 계획과 달라진 것**

| # | 항목 | 내용 |
|---|------|------|
| ① | **봉 조회를 REST → psycopg로** | PLAN은 `supabase-py` + `range()` 페이지네이션을 가정했다. 실제로 42만 행이면 1,000행씩 **426번 왕복**이다. 윈도 함수 한 방으로 23.5초에 끝난다. `ksa_*` 쓰기와 소량 조회는 REST 유지 |
| ② | **`alerts/freshness.py` 신설** | F3 판정을 노드에 넣으면 노드가 두꺼워진다(N11). 순수 함수로 빼서 요일 경계를 테스트로 박았다. **공휴일을 모르므로 연휴 다음 날 오탐 가능** — 일부러 이쪽으로 틀리게 했다(아래 「미해소 이슈 ④」) |
| ③ | **우선주 판정에서 이름 규칙을 뺐다** | SPEC F1은 "티커 끝자리 ≠ 0 **또는** 이름이 우로 끝남"이었다. 이름 규칙을 넣으면 `미래에셋대우` 같은 보통주가 빠진다. **티커 규칙만** 쓴다 (SQL 대조 113건 일치) |
| ④ | **그래프 테스트에 I/O 차단막** | 노드에 실구현을 붙이자 배선 테스트 10개가 실DB에 붙어 깨졌다. `wiring()` 헬퍼가 I/O 노드를 항상 스텁으로 덮는다 (PLAN §6 「금지」) |
| ⑤ | **`Signal`에 랭킹·발송 필드 추가** | `rank_no`·`suppressed`·`sent_kakao`·`sent_email`. 프로즌을 유지하고 `dataclasses.replace()`로 갈아끼운다 — 전략의 순수성을 지키기 위해서다 |

---

## M2 — 전략 5개 ★ TDD 핵심

> 순서: 쉬운 것부터. **VCP를 마지막에** 붙인다 (PLAN §7 — 가장 깨지기 쉽다)

- [x] `alerts/schedule.py` — 실행 주기 판정 (계획의 `base.py`에서 분리, 아래 ①) · 테스트 9개
- [x] `strategies/base.py` — `Checks` 조건 기록기 · `make_signal` · 포맷 헬퍼
- [x] F5 MTF 정배열 — 테스트 15개 (양성 2 + 조건별 음성 5 + 전환일 2 + skip 4 + 점수·주기)
- [x] F8 밴드 스퀴즈 — 테스트 10개
- [x] F6 주봉 눌림목 — 테스트 12개
- [x] F9 장기 턴어라운드 — 테스트 10개 (R1: 횡보 6개월 고정)
- [x] F7 VCP 수축 — 테스트 17개 (거래정지 3개 포함, 아래 ②)
- [x] 전략 노드 5개를 그래프에 fan-out 연결
- [x] **reducer 합류 테스트** — 5개 결과가 전부 모이는 것 확인
- [x] `alerts/rank.py` F10 중복 억제 — 경계일(9/10/11일) 테스트 포함 · 테스트 19개
- [x] `alerts/rank.py` F11 랭킹 — 전략 내 백분위 정규화 · 전략별 최소 1건 보장
- [x] `scripts/dryrun.py` — 과거 N거래일 재현 + **억제 시뮬레이션** (발송·저장 없음)
- [x] **최근 60거래일 드라이런 → 전략별 신호량** (아래 「측정 기록」)
- [x] 임계값 검토 → **조정 불필요로 판단** (억제 후 일 14.7건, 아래 ③)
- [x] **표본 3종목 손계산 대조** — SQL 독립 재계산과 전부 일치 (아래 ④)

**완료 기준 — 전부 충족 (2026-08-17)**
- 전략별 양성·음성 테스트 통과 (**테스트 157개**, ruff·mypy strict 통과)
- 드라이런 신호량 합리적 — 억제 후 **일 14.7건** (0건도 수백 건도 아니다)
- **실DB 전 경로 완주**: `python -m alerts.main` → 69초, 54건 판정·저장, 상위 10건 선정
- **멱등 확인**: 재실행 후에도 `ksa_signals` 54행 그대로 (SPEC N6)
- **저장 확인**: `ksa_signals`에 54건, `evidence` 계약 키 4종(`conditions`·`price`·`volume`·`meta`) 정상

**M2에서 계획과 달라진 것 · 발견한 것**

| # | 항목 | 내용 |
|---|------|------|
| ① | **실행 주기를 요일이 아니라 "주·월이 닫혔는지"로 판정** | "금요일에 돈다"는 규칙은 **금요일이 공휴일인 주를 통째로 건너뛴다.** 대신 데이터 기준일과 실행일이 다른 ISO 주/달에 있는지를 본다 — 마지막 거래일이 목요일이든 수요일이든 정확히 한 번 잡힌다 (`alerts/schedule.py`) |
| ② | ⚠ **거래정지일이 VCP를 도배하고 있었다** | 드라이런에서 발견. KRX 거래정지일은 **거래량 0 · 시고저가=종가**로 들어오는데, 이게 "거래량 0.00배 · 변동성 0.00배"라는 **완벽한 수축**으로 잡혔다. 전 일봉의 **4.5%**가 이런 행이다. 살 수도 없는 종목이라 알릴 이유가 없다 → `strategies.skip_halted`로 **전 전략의 판정일**을 막고, VCP는 수축 구간 전체를 추가로 검사한다. **VCP 판정 545 → 억제 후 176건**으로 정상화 |
| ③ | **눌림목의 MA60 상승 조건에 최소 기울기를 넣었다** | 부호만 보면(`MA60 > 4주 전`) 평평한 종목도 봉 하나에 0.03% 올라 "상승 추세"가 된다. 테스트가 잡아냈다 → **4주간 0.5% 이상**으로 강화 |
| ④ | **`suppress`가 `signals`에 되쓰면 목록이 두 배가 된다** | `signals`에는 `operator.add` 리듀서가 걸려 있다. 작업본은 `ranked` 키로 넘긴다 |
| ⑤ | 임계값 조정 없음 | 억제 후 일 14.7건이면 카카오 상위 10건 + 메일 전체로 적정하다. SPEC §4-2 초기값을 그대로 확정 |

---

## M3 — 발송

- [x] `alerts/render.py` — 카카오 요약 본문 (200자 경계 · 0건 문구 · 절단 표시 · 경고 보존)
- [x] `alerts/render.py` — 메일 제목·HTML·평문 (근거값 전개 · **HTML 이스케이프** · 헤더 개행 제거) · 테스트 21개
- [x] `notify/tls.py` — 공용 TLS 컨텍스트 (계획의 `base.py` 대신, 아래 ②)
- [x] `notify/kakao.py` — 발송 + 토큰 갱신 + `KOE322` 시 명시적 안내
- [x] `notify/email.py` — Gmail SMTP(587 STARTTLS) + 앱 비밀번호
- [x] `scripts/kakao_auth.py` — 최초 인가 (`--force` · `--check` 지원)
- [x] F13c — 발송 노드 2개를 **순차** 연결(메일 → 카카오). 둘 다 예외를 밖으로 내지 않는다 (아래 ①)
- [x] `record_run` → `finalize` — 기록을 먼저 하고 **실패 판정은 `finalize` 한 곳에서만** (N5)
- [ ] **카카오 실발송 확인** — ⏸ `KAKAO_REFRESH_TOKEN` 발급 대기 (사용자 브라우저 동의 필요)
- [ ] **메일이 받은편지함에 도착했는지 확인** — ⏸ 수신자가 `dayhyub@naver.com`이라 사용자 확인 필요 (R11)

**여기까지 실제로 확인한 것 (2026-08-17)**

| 확인 | 결과 |
|------|------|
| 메일 실발송 | ✅ **54건 발송 성공.** 제목 `[신호 54건] 08/14 — MTF 정배열 5 · 주봉 눌림목 2 · VCP 수축 47`, 본문에 조건별 근거값 전개 확인 |
| 채널 실패 격리 | ✅ 카카오가 토큰 없이 실패해도 **그래프가 완주**하고 `record_run`에 도달 |
| 실패 기록 | ✅ `ksa_runs`에 `status=send_failed` + 오류 메시지까지 남음 |
| 시끄러운 실패 | ✅ 종료 코드 **1** (N5) |
| 성공 기록 | ✅ `status=ok` · `sent_email_n=54` |

**남은 2건은 사람이 해야 하는 일이다** — 브라우저 동의와 네이버 받은편지함 확인.

**M3에서 계획과 달라진 것 · 발견한 것**

| # | 항목 | 내용 |
|---|------|------|
| ① | **발송을 병렬 → 순차로 바꿨다** | 병렬이면 두 노드가 **서로의 실패를 모른다.** F13c의 "살아 있는 채널이 죽은 채널을 알려 준다"가 성립하지 않는다. 발송은 전체 70초 중 2~3초라 병렬로 얻는 게 없다. **메일 먼저** — 내용을 다 담는 쪽이고 토큰 만료 같은 조용한 실패 모드가 없다. 카카오가 뒤따르며 `⚠ 메일 발송 실패`를 실어 나른다. SPEC F13c·PLAN §1-1 갱신 |
| ② | **`notify/base.py` 대신 `notify/tls.py`** | 계획의 Channel 프로토콜은 만들지 않았다 — 채널이 둘뿐이고 시그니처가 서로 달라(카카오는 200자 텍스트, 메일은 제목+본문 2종) 억지로 맞추면 오히려 얇아지지 않는다. 대신 실제로 공유가 필요했던 것은 **TLS 컨텍스트**였다 |
| ③ | ⚠ **macOS 파이썬에 CA 번들이 없다** | 첫 실발송에서 `CERTIFICATE_VERIFY_FAILED`. 선행 프로젝트에 기록돼 있던 함정을 카카오 쪽에만 반영해 뒀다가 메일에서 그대로 밟았다. `certifi`를 명시적으로 쓰는 공용 컨텍스트로 정리 |
| ④ | **카카오 링크를 본문이 아니라 `link` 객체로** | 200자 예산을 URL이 다 먹는다. 템플릿의 `link.web_url`은 본문 글자 수에 안 들어간다 |
| ⑤ | **`record_run`의 DB 쓰기 실패는 삼킨다** | 기록 실패가 알림 자체를 실패로 만들면 안 된다. 경고만 찍고 넘어간다 |

---

## M4 — 자동화

- [ ] `alerts/main.py` CLI — `--date` · `--channel kakao|email|both` · `--dry-run`
- [ ] F3 데이터 신선도 검사 (기준일 불일치 시 발송 중단·실패)
- [ ] `.github/workflows/alert.yml` — cron `0 23 * * 0-4` + `workflow_dispatch`
- [ ] GitHub Secrets 등록 (Supabase · 카카오 · Gmail 앱 비밀번호)
- [ ] **리프레시 토큰 Secret 자동 갱신** (R2) — 권한 방식 결정 후 구현
- [ ] 워크플로 권한 최소화(`permissions:` 명시) · 로그 시크릿 미노출 확인
- [ ] 신선도 검사 실동작 시험 (기준일 조작)

**완료 기준**: 수동 실행 성공 → **연속 5거래일 두 채널 도착** (0건인 날 "없음" 포함)

---

## M5 — 웹 `/signals`

> ⚠ **`docs/DESIGN.md` 시안 사용자 합의 전에는 화면 구현을 시작하지 않는다** (SPEC N8)

- [ ] DESIGN.md §5 UI 시안(HTML 목업) 작성 → **사용자 합의**
- [ ] Next.js 16 + TS(strict) + Tailwind 초기화 (`web/`에서 npm 실행 — 루트 오설치 주의)
- [ ] Supabase 읽기 클라이언트 (anon 키)
- [ ] F15 신호 목록 — 전략별 그룹 · 필터 · 정렬
- [ ] F16 신호 상세 — `evidence.conditions` 렌더
- [ ] F17 이력 조회 — 과거 날짜
- [ ] Vitest 컴포넌트 테스트
- [ ] Vercel 배포
- [ ] 카카오·메일 본문의 웹 링크를 실 URL로 교체

**완료 기준**: `npm run lint` · `npm test` · `npm run build` 통과 · 배포 URL 실동작

---

## M6 — 마무리

- [ ] 보안 점검 (N7) — 보안 리뷰 · 시크릿 노출 · CI 권한/아티팩트
- [ ] `docs/GRAPH.md` 최종 갱신 (`scripts/export_graph.py` 재실행 — N12)
- [ ] `CLAUDE.md` 최종 갱신 (겪은 함정 기록)
- [ ] `README.md` (영어) · `README_KO.md` (한국어) + 상호 링크
- [ ] 워크스페이스 `../CLAUDE.md` 프로젝트 목록 표에 추가
- [ ] 운영 기록 — 첫 2주 도착 여부 점검

---

## 측정 기록

> 실측값을 여기에 남긴다. 추정으로 판단하지 않기 위해서다.

| 항목 | 값 | 측정일 |
|------|-----|--------|
| 전 종목 | 2,763 (SQL 카운트와 일치 — 절단 없음) | 2026-08-17 |
| **유니버스 (제외 후)** | **1,192종목** | 2026-08-17 |
| 제외: 스팩 / 우선주 / 유동성 미달 | 71 / 113 / **1,387** | 2026-08-17 |
| 데이터 기준일 조회 `max(d)` | 16.0s ⚠ (아래 참조) | 2026-08-17 |
| 20일 평균 거래대금 집계 | 34.6s | 2026-08-17 |
| 전 종목 봉 조회 (42만 행) | **23.5s** | 2026-08-17 |
| `bars` 상태 메모리 (PLAN 추정 200~400MB) | **130MB** — 추정보다 작다 | 2026-08-17 |
| **배치 전체 소요** | **50.5s** (R7 목표 5분 이내 ✓) | 2026-08-17 |
| 봉 충족률 (일120 / 주60 / 월20) | 98% / 95% / 94% | 2026-08-17 |
| 배치 전 경로 소요 (저장 포함) | **69s** | 2026-08-17 |

**60거래일 드라이런** (2026-05-20 ~ 08-14 · 유니버스 1,192종목 · 51초)

| 전략 | 판정 | **억제 후** | 일평균 | 신호난 날 | 억제창 |
|---|---|---|---|---|---|
| mtf | 602 | **490** | 8.2 | 58일 | 10일 |
| pullback | 240 | **206** | 3.4 | 12일 | 28일 |
| vcp | 545 | **176** | 2.9 | 41일 | 10일 |
| squeeze | 31 | **7** | 0.1 | 6일 | 10일 |
| turnaround | 4 | **4** | 0.1 | 2일 | 90일 |
| **합계** | 1,422 | **883** | **14.7** | | |

> **"억제 후"가 실제로 알림에 담기는 건수다.** 일 14.7건이면 카카오 상위 10건 + 메일 전체로 적정하다.
> `turnaround` 0.1건/일은 **고장이 아니라 월봉 3년 제약의 예정된 결과**다 (SPEC R1).
> `squeeze`도 원래 드문 패턴이다.

**20일 평균 거래대금 분포** (2,763종목) — 유동성 하한을 정한 근거

| 백분위 | 10% | 30% | **50%** | 70% | 90% |
|---|---|---|---|---|---|
| 거래대금 | 0.3억 | 1.2억 | **3.6억** | 11.9억 | 104억 |

| 하한 | 1억 | 2억 | 3억 | **5억** | 10억 | 20억 |
|---|---|---|---|---|---|---|
| 유니버스 | 1,990 | 1,634 | 1,448 | **1,192** | 864 | 628 |

> 중앙값이 3.6억이므로 **5억은 "유동성 상위 절반"** 에 해당한다. 극단적 필터가 아니다.
> 초기값으로 유지하고 M2 드라이런에서 신호량을 보고 조정한다 (SPEC R8).

---

## 트러블슈팅 기록

> 겪은 오류와 원인을 남긴다. 같은 함정을 두 번 밟지 않기 위해서다.

| 일자 | 증상 | 원인 | 해결 |
|------|------|------|------|
| 2026-08-17 | `ksc_bars`를 `count="exact"`로 세면 `57014 statement timeout` | 240만 행 전수 스캔. Supabase 기본 statement timeout을 넘긴다 | **`ksc_bars`에 exact count를 쓰지 않는다.** 완결성 검사는 psycopg로 SQL 집계 |
| 2026-08-17 | `ksc_meta.update`가 `tickers: 200`인데 유니버스는 2,763 | 종목 확장 이전 실행 기록이 남아 있다. 메타는 **마지막 실행이 무엇을 했는지**를 적을 뿐 데이터 최신 여부를 보증하지 않는다 | **F3을 `ksc_bars.max(d)` 기준으로 바꿨다** (SPEC v1.1.1). 표본 12종목 확인 결과 실제 데이터는 전부 2026-08-14로 최신 |
| 2026-08-17 | `mypy --strict`에서 `add_node` 오버로드 불일치 | LangGraph의 `_Node` 제네릭이 평범한 `Callable[[AlertState], Any]`를 못 받는다 | `graph.pick()` 반환형만 `Any`. 프레임워크 경계에서만 완화 |
| 2026-08-17 | ruff가 `from supabase import ...`를 로컬 패키지 그룹으로 옮긴다 | venv에 설치돼 있어도 프로젝트 루트 기준으로 판단한다 | `pyproject.toml`에 `known-third-party` 명시 |
| 2026-08-17 | 노드에 실구현을 붙이자 그래프 테스트 10개가 깨짐 | 배선 테스트가 기본 노드를 그대로 써서 **실DB에 붙었다** | `tests/test_graph.py`의 `wiring()`이 I/O 노드를 항상 스텁으로 덮는다. `IO_NODES`에 새 I/O 노드를 추가하는 것을 잊지 말 것 |
| 2026-08-17 | 유니버스 순서 테스트가 실패 | 테스트 픽스처 티커가 `111111`처럼 끝나 **우선주로 걸러졌다.** 코드가 아니라 테스트가 틀렸다 | 픽스처 티커를 0으로 끝나게 수정 |
| 2026-08-17 | `select max(d) from ksc_bars`가 16초 | PK가 `(ticker, timeframe, d)`라 `d` 단독 최대값을 인덱스로 못 받는다. 190만 행 스캔 | 지금은 감수한다(전체 69초 중 16초). 느려지면 `ksc_meta`를 **힌트**로 쓰고 `max(d)`로 **검증**하는 2단 구조로 바꾼다 |
| 2026-08-17 | VCP가 하루 16건씩 쏟아지고 근거가 "거래량 0.00배·변동성 0.00배" | **거래정지일**. KRX는 거래량 0 · 시고저가=종가로 준다. 장대양봉 직후 정지되면 완벽한 수축으로 보인다. 전 일봉의 4.5%가 이런 행 | `strategies.skip_halted`가 전 전략의 판정일을 막고, VCP는 수축 구간 전체를 검사한다. **테스트 3개로 못 박았다** |
| 2026-08-17 | 눌림목이 평평한 종목에서도 잡힘 | `MA60 > 4주 전 MA60`을 부호로만 봤다. 봉 하나에 0.03%만 올라도 참이 된다 | 4주간 **0.5% 이상**으로 강화 (`TREND_MIN_SLOPE`) |
| 2026-08-17 | 전략 테스트 픽스처가 계속 틀림 (MTF 4건, 스퀴즈 4건) | 시나리오가 조건을 실제로 만족하지 않았다 — 완만한 상승은 **어제 이미 정배열**, 스퀴즈는 봉이 1개 모자람 | **코드가 아니라 테스트가 틀린 경우다.** 실패를 보고 바로 구현을 고치지 말 것 |
| 2026-08-17 | 첫 메일 발송에서 `CERTIFICATE_VERIFY_FAILED` | **macOS Python.org 설치본에 CA 번들이 없다.** curl은 되는데 파이썬만 실패한다. 카카오 쪽에만 대비해 뒀다가 메일에서 그대로 밟았다 | `alerts/notify/tls.py` 공용 컨텍스트가 `certifi`를 쓴다. 시스템 전체를 고치려면 `/Applications/Python 3.13/Install Certificates.command` 1회 실행 |
| 2026-08-17 | 발송 노드를 붙이자 그래프 테스트 10개가 또 깨짐 | `wiring()`이 발송·기록 노드를 안 덮어서 실네트워크·실DB에 붙었다. **M1과 같은 실수를 반복** | `IO_NODES`에 더하는 것을 잊지 말 것. `record_run`은 상태 판정 로직만 남긴 스텁으로 대체 |

### 미리 알고 있는 함정 (선행 프로젝트에서 확인됨)

| 함정 | 출처 |
|------|------|
| Supabase REST는 **1000행에서 오류 없이 잘린다**. 완결성 검사는 SQL로 | krx-stock-charts (오탐 "424개 누락" 실제 14개) |
| 티커 `.in()` 필터가 URL에 실린다. 2,763개 = 19KB → **400**. 300개씩 청크 | 동상 |
| 티커는 숫자가 아니다 — `0126Z0` 실재. `^[0-9A-Z]{6}$` | 동상 |
| 카카오 `KOE004`(로그인 비활성) → `KOE205`(동의항목) → `KOE006`(리다이렉트 URI는 **앱 설정 > 플랫폼 키 > REST API 키**에 등록) → `insufficient scopes`(`--force`로 재동의) | krx-strategy-alerts |
| macOS Python.org 설치본은 **CA 인증서가 없다**. `Install Certificates.command` 1회 실행 | 동상 |
| pip/npm은 **반드시 프로젝트 루트에서** (워크스페이스 루트 오설치 사례 있음) | 워크스페이스 CLAUDE.md |
| LangGraph 병렬 노드가 같은 상태 키를 쓰면 **reducer 없이는 조용히 덮어쓴다**. 예외가 안 나서 못 알아챈다 | PLAN §6 — 전용 테스트로 방어 |

---

## 미해소 이슈

| ID | 내용 | 상태 |
|----|------|------|
| ① | ~~anon 키가 다른 테이블 12개를 **읽는다**~~ → **실측 결과 더 심각.** 2026-08-17 권한을 직접 조회한 결과, RLS가 꺼진 테이블 **27개**에 anon이 `SELECT`뿐 아니라 **`INSERT`·`UPDATE`·`DELETE`·`TRUNCATE`** 권한을 갖는다 (`stocks`, `nq_questions`, `libraries`, `translations`, `shareholder_data_anal` 등). 배포된 웹의 anon 키는 공개돼 있다 | ⚠ **사용자 판단 필요.** 기존 수용(SPEC R4)은 "읽기"를 전제한 것이었다. **이 프로젝트의 `ksa_*`는 안전하다** — RLS on + SELECT 정책만이라 anon 쓰기가 실제로 차단되는 것을 확인했다. 노출을 늘리지는 않았으나, 27개 테이블의 쓰기 노출은 이 프로젝트 밖의 별건 |
| ② | Supabase 무료 한도 500MB 중 385MB 사용 (여유 115MB) | 인지. `ksa_*`는 연 수 MB로 영향 작음 (SPEC R9) |
| ③ | 월봉 3년(37개) → F9 판정 창 19개월, 신호가 드물다 | **의도된 선택** (D3). 월봉만 10년 확대로 언제든 해소 가능 (SPEC R1) |
| ④ | **F3이 공휴일을 모른다** — 연휴 다음 날 멀쩡한 데이터를 "낡음"으로 볼 수 있다 | **의도된 방향의 오류.** 잘못 "최신"이면 낡은 데이터로 만든 신호를 보내고, 잘못 "낡음"이면 그날 "데이터 지연" 한 통이 올 뿐이다. M4에서 cron을 실제로 돌려 보고 오탐이 잦으면 공휴일 목록을 붙인다 |
| ⑤ | 유동성 하한 5억이 2,763종목 중 1,387개를 뺀다 | **M2 드라이런에서 재검토** (SPEC R8). 중앙값 3.6억 기준 "상위 절반"이라 과하지는 않으나, 신호가 너무 적으면 3억으로 낮춘다 |
